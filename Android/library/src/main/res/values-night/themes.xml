<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- dark theme -->

    <style name="Theme.Felwal" parent="Theme.Felwal.Base">
        <!-- background -->
        <item name="android:colorBackground">@color/fw_felwal_background_dk</item>
        <item name="colorOnBackground">@color/fw_white</item>
        <item name="colorSurface">@color/fw_felwal_surface_dk</item>
        <item name="colorOnSurface">@color/fw_white</item>

        <item name="android:listDivider">@color/fw_felwal_divider_dk</item>
        <!-- TODO: remove? -->
        <item name="strokeColor">@color/fw_felwal_divider_dk</item>
    </style>

</resources>