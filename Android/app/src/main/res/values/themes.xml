<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- default theme -->

    <style name="Theme.Default" parent="Theme.Felwal">
        <item name="android:statusBarColor" tools:targetApi="l">?android:colorBackground</item>
    </style>

    <!-- light theme -->

    <style name="Theme.Demo" parent="Theme.Default">
        <item name="android:windowLightStatusBar">true</item>

        <!-- primary -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorOnPrimary">?android:colorBackground</item>

        <!-- secondary -->
        <item name="colorSecondary">@color/teal_700</item>
        <item name="colorOnSecondary">?android:colorBackground</item>

        <item name="dm_demoButtonColor">@color/button_bg_lt</item>
    </style>

</resources>